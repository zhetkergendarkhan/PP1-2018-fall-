#include<iostream>
using namepspace std;
int main()
{
	string s;
	cin>>s;
	for(int i=0;i<s.size();i++)
		if(islower(s[i]))
			s[i]=toupper(s[i])
		else if(toupper(s[i]))
			s[i]=islower(s[i])
		return 0;
	}